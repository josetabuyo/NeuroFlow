# ✅ COMPLETADO: Dos máscaras de diagnóstico simples

Se agregaron `MASK_ALL_EXC` (`"all_exc"` / "Todo Exc") y `MASK_ALL_INH` (`"all_inh"` / "Todo Inh") al inicio de `MASK_PRESETS` en `backend/core/masks.py`. Aparecen como primeras opciones en el selector de conexionado del sidebar. Tests: 193 passed, 2 skipped (los skips son intencionales para las masks de diagnóstico puro).

---

# Próxima tarea: (pendiente de definir)
